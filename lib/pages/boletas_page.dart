import 'package:flutter/material.dart';

class BoletasPage extends StatelessWidget {
  const BoletasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Registrar una Bosadasdaddsleta'),
    );
  }
}